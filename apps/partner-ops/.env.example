# Public Platform URL: login redirect and, by default, Partner server calls.
PLATFORM_ORIGIN="http://localhost:3000"
# Optional only in private networking (Docker/Kubernetes); otherwise omit it.
# PLATFORM_INTERNAL_ORIGIN="http://platform:3000"
PARTNER_OPS_ASSET_PREFIX="/partner-ops-static"
