# @debtflow/react-app-shell

React-only product chrome for Next/React standalone apps. In composed mode public Platform renders it once and embedded remotes render content only. Angular/Vue must use neutral navigation/design tokens, never this React package.
